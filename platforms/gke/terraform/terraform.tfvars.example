# Copy to terraform.tfvars (gitignored) and fill in your own project.
# `gcloud config get-value project` shows your current default, but this
# configuration deliberately does not inherit it — set it explicitly.
project_id = ""

# Defaults below match this configuration's own variables.tf — override
# only what your project's quota or preferences require.
# region                     = "asia-southeast1"
# zone                       = "asia-southeast1-a"
# cluster_name               = "gke-nextops-production-sgp-001"
# system_node_machine_type   = "e2-standard-2"
# workload_node_machine_type = "e2-standard-4"
# workload_min_count         = 1
# workload_max_count         = 4
# artifact_registry_repo_id  = "nextops-gke-sgp-001"
